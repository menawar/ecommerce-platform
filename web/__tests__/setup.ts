// Register @testing-library/jest-dom's custom matchers (toBeInTheDocument,
// toHaveTextContent, etc.) so every test file can use them without a per-file import.
import "@testing-library/jest-dom/vitest";
