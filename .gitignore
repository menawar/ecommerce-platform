# Compiled binaries / build output
/bin/
/healthcheck
*.exe
*.test
*.out

# Go workspace sum (regenerated; safe to ignore early, revisit if it matters)
go.work.sum

# Local env / secrets — never commit these
.env
*.local

# Editor / OS noise
.DS_Store
.idea/
.vscode/

# Stray design export kept locally for reference, not part of the app
web/E-commerce platform frontend design (2)/
